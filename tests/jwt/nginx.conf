daemon off;
error_log logs/error.log debug;
pid logs/nginx.pid;

events {
    worker_connections 64;
}

http {
    access_log logs/access.log;

    server {
        listen 8888;

        location / {
            # TODO: Enable when jwt module is implemented
            # jwt on;
            # jwt_secret "test-secret-key-256-bits-long!!";
            return 200 "jwt module placeholder\n";
        }

        location /protected {
            # jwt on;
            return 200 "protected resource\n";
        }
    }
}
