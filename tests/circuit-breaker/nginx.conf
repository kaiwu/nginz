daemon off;
error_log logs/error.log info;
pid logs/nginx.pid;

events {
    worker_connections 64;
}

http {
    access_log logs/access.log;

    server {
        listen 8888;

        location / {
            # TODO: Enable when circuit-breaker module is implemented
            # circuit_breaker on;
            # circuit_breaker_threshold 5;
            return 200 "circuit-breaker module placeholder\n";
        }
    }
}
