daemon off;
error_log logs/error.log debug;
pid logs/nginx.pid;

events {
    worker_connections 64;
}

http {
    access_log logs/access.log;

    server {
        listen 8888;

        location /api/ {
            pgrest_pass "host=10.255.255.1 port=15432 dbname=testdb user=postgres connect_timeout=1";
            pgrest_schemas "public";
        }
    }
}
